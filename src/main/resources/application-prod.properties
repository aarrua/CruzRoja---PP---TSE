# Configuración de la aplicación
spring.application.name=practicas-inscripcion

# Conexión a Supabase (Copiar la URL de tipo JDBC desde el panel)
spring.datasource.url=jdbc:postgresql://db.igmzzdjmzjdpwwkcrlaf.supabase.co:5432/postgres
spring.datasource.username=postgres
spring.datasource.password=8*UP.%NKyFmA*4R
spring.datasource.driver-class-name=org.postgresql.Driver

# Configuración de Hibernate / JPA (Mantiene las tablas actualizadas en la nube)
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# Credenciales de acceso a la web (Se mantienen igual para el operador)
spring.security.user.name=cruzroja
spring.security.user.password=cruzroja_2da_vuelta
spring.security.user.roles=USER,ADMIN